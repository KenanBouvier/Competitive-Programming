#include <bits/stdc++.h>

using namespace std;

void solve(){

    int s; cin>>s;
    vector<int> seasons(s);
    vector<int> duration(s);

    for(int i = 0;i<s;i++){
        int a;cin>>a;
        duration[i] = a;
    }
    
}


int main(){
    ios::sync_with_stdio(false); cin.tie(NULL);
    int t; cin>>t;
    while(t--){
    solve();
    
    }

    return 0;
}